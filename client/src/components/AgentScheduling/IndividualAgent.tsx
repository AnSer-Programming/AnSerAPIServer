import React, {useState, useEffect} from 'react';

const IndividualAgent = (data:any) => {
  const [selectedAgent, setSelectedAgent] = useState<string>("None");
  useEffect(()=>{},[selectedAgent]);

}

export default IndividualAgent;